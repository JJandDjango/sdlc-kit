# State - sdlc_development_kit

> **Contract** - one question: *what is in flight right now?*
> <=1 page - regenerate at every session end - disposable, always safe to overwrite.
> _Generated 2026-07-30 (session 21 close)._

## Now
- Session 21 (2026-07-30): **the G4 mechanical core shipped end to
  end** - roadmap step 3. Contract `dotnet-profile-g4` ready-green at
  intake on the first pass (F1-F10 ratified whole; venue, gitleaks
  binding, and battery-CWE disposition ruled in-contract). Four
  conditions bound: sdlc-dotnet.yml became the merge gate (merge_group
  authoritative / pull_request advisory; job renamed inner-loop ->
  merge-gate), carrying the echo as G4.1's three clauses, dotnet test
  with the zero-tests guard (G4.11), gitleaks diff-mode + NuGetAudit
  over a locked graph (G4.9.1-2), and the four-vector suppression
  audit (G4.10) as `python -m taskcontract suppression-audit` - the
  kit's first pipeline-native module, shipped behind the pin per ADR
  0020's distribution rule. Props gained the locked-graph block
  (merge-target; brownfield lands red at restore, deliberately).
- Receipts: suite 111 -> 146; both audit engines clean; docs Pass-0
  red -> green at ship; kit 0.7.0. PR #18 merged (819686b), v0.7.0
  tagged there; self-pin PR #19 merged (eb881dc), its contracts job
  proving `pip install @v0.7.0`; update.py reads scaffold-current.
  Session-20 close merged first (PR #17, 3303913).
- Battery-CWE map re-registered (Husky.NET precedent) behind "first
  G4.7 scanner slice or next taxonomy session". G4 deferrals named in
  the fit notes: license allowlist, SLA backstop, quarantine-list
  content, the seven unbound conditions (G4.2-G4.8).
- Prior close: #20 (18ad538) - G3 slice, 0.6.0, register verdict.

## Blockers
- None.

## Next actions
1. **Per-gate agent arc** (carried): harness, venue, verdict
   plumbing, plugin agents/ distribution - the suppression audit's
   0/1/2 + `--json` contract is the first verdict surface to build on.
2. **Pilot M0 session** (engine repo): first profile-aware consumer -
   the dotnet payload now spans G3 + G4 core; Q5 reality data.
3. Carried one-liners, two remaining: README.md tree line,
   CONVENTIONS.md schema ref (both still name the old root schemas/
   path).
4. Deferrals unchanged: Q4 numbers, PyPI publish (trigger stands),
   explainer PDF, V8 RDF map; gaps behind triggers: Azure DevOps
   variant, Husky.NET, dotnet-tool wrapper, battery-CWE map (new).

## Open questions
- Q4 thresholds, numeric only (G4.8 ratchet budgets ride these).
- Q5 two-channel decorrelation - M0 remains the first live data point.
- Q6 first analyzer tranche - rides pilot activation (ADR 0009's
  selection function needs real defect data).
- Per-gate agent shape: venue, context assembly, verdict format,
  plugin versioning.
