# SDLC kit - per-repo configuration (written by /sdlc init, {{ date }}).
kit: {{ kit_ref }}
material: {{ material }}  # greenfield | brownfield
stack: {{ stack }}  # free text; selects the tooling profile at activation
active_gates:
  - G0  # contract conformance - CI backstop; /sdlc intake is the venue
# Gates join this list as the kit's activation program builds their
# enforcement: {{ kit_repo }}/blob/main/docs/gates.md
