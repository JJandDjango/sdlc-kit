# State - sdlc_development_kit

> **Contract** - one question: *what is in flight right now?*
> <=1 page - regenerate at every session end - disposable, always safe to overwrite.
> _Generated 2026-07-24._

## Now
<!-- What's actively being worked. -->
- Session 9 (2026-07-24) closed: the G5+G6 walk ran as an eight-stop
  interactive tour (frame -> pacts -> oracle pair -> concurrency ->
  mutation -> trace conformance -> human boundary -> close-out) plus a
  mid-walk cadence amendment; every ruling ratified in-conversation
  before its edit landed. Wrap committed `4ec2206` - pages
  `docs/gates/G5-integration-system.md` + `G6-uat-staging.md`, registry
  now 33 specified / 52 total (G5 roster 6 -> 7, G6 2 -> 3).
- Ruled: frame - snapshot pinning + verified-artifact promotion; red
  seizes promotion never merges, stop-the-line intake escalation past
  window (fix lane via contract reference); fresh exploration with
  mandatory replay recipes, no retry-to-green; subject integrity
  inherited from G4; cadence = budget-class, schedule = policy
  ("nightly" demoted to reference default). Oracle-designation rubric
  (no new ADR): per-component declaration record at G1, reviewed as
  G1.3 item 11; must-fuzz has no opt-out (trust-boundary parsers +
  native interop - the taxonomy `-` row closed via G5.3). G5.5 floor
  procedure fixed (ratio + zero-survivor small-N; class-E dismissals,
  in-source ignores banned -> G4.10 list; direction-conditional
  overrides; numbers stay Q4). G5.6 conformance criteria fixed
  (safety-side admissibility; action coverage doubles as the
  gutted-instrumentation detector; mapping totality; Coyote piggyback
  capture). G5.1 accepted-pact lane (consumers drive the agenda, never
  the gate). G6: the principal grades (non-delegable; agent scouting =
  suggestions only), delta-scope criteria walk, findings indict the
  spec layer by construction, accept-with-findings = auto-armed
  forward obligation; acceptance record class S, G7-consumed,
  default-deny.
- Adoptions: G5.7 soak/resource-trend (perf split: trend shapes at G5,
  absolute budgets stay G7); G6.3 venue certification (runtime drift
  outside 0010's sight; every walk rehearses the G7 deploy). Rejected:
  chaos-as-condition (routed: G1.3 item 8 extended per external
  dependency; failure actions in hard-core models; production chaos ->
  G8). Zero new ADRs - both schedule-forced decisions landed as page
  rulings. Cascades: taxonomy (2 rows), G1 checklist (+item 11 +
  item-8 clause), G4.10 construct list (Stryker ignores). Cairn audit
  clean (7 INFO gate-page orphans, by design - grew by exactly the two
  new pages).

## Blockers
<!-- What's stopping progress. -->
- None.

## Next actions
<!-- The ordered next steps. -->
1. Next session (S10): the G7 + G8 walk (7 conditions; refresh plan.md
   at its start). Banked inputs: G5.1 verification matrix
   (can-i-deploy); the G6 acceptance record as G7's admission input
   (record present + accepted + no unresolved blocking findings); the
   soak split's budget half (G7.1 owns absolute budgets on quiet
   infra); G6.3 deploy-rehearsal formalized from G7's side; G5.6 <->
   G8.1 shared trace instrumentation; production chaos (G8 venue
   question); G8.3 reuses the G6.2 conversion-record shape. Schedule
   row: benchmark-budget + SLO procedure shapes likely forced.
2. S11 inputs banked: G9 tightening-job shape (0010
   direction-conditional lane - now also corpus growth and
   mutation-floor moves); G9.2 SLA windows (consumed by G4.9's
   soft-seize AND the G5 red-window escalation); stop-the-line
   mechanics incl. the contract fix-lane field (0005 touch); G9.3
   license-sweep division; Q4 numbers (mutation floor + small-N
   cutoff, ratchet budgets + capture parameters, tightening cadence).
3. S12 inputs banked: Q7's worked example (0010/G4.8 channel-weight
   tiering); PL-PIPE.2 fixture pattern (traceability + battery golden
   tests).
4. Parked G0 work waits on Q6 pilot: F10 `/intake` (venue live flips
   G0.1 -> `enforced`) and F11 scaffold. Build-item register stays
   four (0008); one conditional noted on the G5 page:
   trace-validation harness on first hard-core activation.

## Open questions
<!-- Unresolved decisions that need an answer. -->
- Q4 thresholds, numeric only (procedures now fixed through G5):
  mutation floor + small-N cutoff (G5.5), complexity budgets + capture
  parameters (G4.8), tightening cadence (G9).
- Q5 two-channel decorrelation (harness design); named sub-question:
  what the Developer's context contains - test source vs criteria +
  diagnostics.
- Q6 pilot repository selection; greenfield vs retrofit - gates
  F10/F11, G0.1's `enforced` flip, the analyzer first tranche, the
  four build items, G4.7's and G5.5's bootstrap variants, and the
  first oracle-designation records.
- Q7 enforcement-layer change-control workflow (PL-PIPE.1); worked
  example on record.
- Protect-main still blocked for this repo: rulesets on a private repo
  need GitHub Pro - upgrade, make public, or accept unprotected main?
